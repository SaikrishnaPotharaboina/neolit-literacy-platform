NEOLIT PROJECT - HOW TO RUN
===========================

This guide explains how to run the NeoLit literacy learning platform locally on Windows.

PROJECT FOLDERS
---------------

Backend  = FastAPI Python API and database code
frontend = React and Vite web application

The application uses:

- Frontend: React, Vite, Tailwind CSS, React Router, Axios
- Backend: Python, FastAPI, SQLAlchemy, Pydantic, JWT
- Database: SQLite for local development; MySQL for production

1. REQUIREMENTS
---------------

Install these programs before running the project:

- Python 3.11 or newer
- Node.js 18 or newer
- npm, included with Node.js
- MySQL 8 for production use (not required for the local SQLite setup)

Check that Python and Node.js are installed:

    python --version
    node --version
    npm --version

If the command "python" is not available on Windows, install Python from python.org and enable "Add Python to PATH" during installation.

2. OPEN THE PROJECT
-------------------

Open PowerShell in the downloaded project folder:

    cd "C:\Users\DELL\Desktop\Duoling website"

Your folder should contain:

    Backend
    frontend

3. SET UP THE BACKEND
---------------------

Go to the backend folder:

    cd Backend

Create a Python virtual environment if one does not already exist:

    python -m venv .venv

Activate the virtual environment:

    .\.venv\Scripts\Activate.ps1

If PowerShell blocks activation, run PowerShell as your normal user and use this command once:

    Set-ExecutionPolicy -Scope CurrentUser RemoteSigned

Then activate again:

    .\.venv\Scripts\Activate.ps1

Install all backend packages:

    python -m pip install --upgrade pip
    python -m pip install -r requirements.txt

The requirements file installs:

- fastapi: backend web framework
- uvicorn: development web server
- sqlalchemy: database ORM
- pymysql: MySQL database driver
- pydantic and pydantic-settings: validation and environment settings
- python-jose: JWT token support
- passlib and bcrypt 4.0.1: secure password hashing
- python-dotenv: .env file support
- alembic: database migration support
- pytest and httpx: backend testing

4. BACKEND ENVIRONMENT FILE
---------------------------

The local backend uses SQLite through Backend/.env.

Example local settings:

    DATABASE_URL=sqlite:///./neolit_v2.db
    SECRET_KEY=local-development-secret-change-before-deploying
    ALGORITHM=HS256
    ACCESS_TOKEN_EXPIRE_MINUTES=60

Do not commit a real .env file containing production secrets.

For MySQL production, set DATABASE_URL to a value like:

    DATABASE_URL=mysql+pymysql://literacy_user:password@localhost/literacy_platform

5. START THE BACKEND
--------------------

Run this command from the Backend folder while the virtual environment is active:

    python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

The backend will be available at:

    http://localhost:8000

Check that it works by opening:

    http://localhost:8000/health

Expected response:

    {"status":"ok"}

Keep this terminal open.

6. SET UP THE FRONTEND
----------------------

Open a second PowerShell terminal and run:

    cd "C:\Users\DELL\Desktop\Duoling website\frontend"
    npm install

7. START THE FRONTEND
---------------------

Run:

    npm run dev

Open the URL shown by Vite, normally:

    http://localhost:5173

8. USE THE APPLICATION
----------------------

1. Open Register and create an account.
2. Log in with the same email and password.
3. Complete your learning profile.
4. Choose a language and level.
5. Open lessons and assessments.

9. TEST THE PROJECT
-------------------

Frontend production build:

    npm run build

Backend tests, from the Backend folder:

    python -m pytest -q

10. COMMON PROBLEMS
-------------------

Problem: "python is not recognized"

Solution: Install Python and enable "Add Python to PATH". Restart VS Code or PowerShell afterward.

Problem: "npm is not recognized"

Solution: Install Node.js, then restart VS Code or PowerShell.

Problem: Login says "Invalid email or password"

Solution: Confirm that registration completed successfully and that you are using the exact same email and password. Passwords are case-sensitive.

Problem: The dashboard cannot load the learning space

Solution: Confirm that the backend is running and that http://localhost:8000/health returns {"status":"ok"}.

Problem: Port 8000 is already in use

Solution: Start the backend on another port:

    python -m uvicorn app.main:app --reload --port 8001

Then set the frontend API URL before starting Vite:

    $env:VITE_API_BASE_URL="http://localhost:8001"
    npm run dev

Problem: Registration returns a password hashing error

Solution: Install the backend requirements again:

    python -m pip install -r requirements.txt

FIRST ADMIN ACCOUNT
-------------------

For local development, create the first admin from the Backend folder:

    python create_admin.py

For the deployed application, set ADMIN_SETUP_KEY in Render and open:

    https://your-frontend-domain.vercel.app/register/admin

The setup key can create only the first admin account. After that, use the admin dashboard to create more accounts.


@router.post("/logout")
def logout_user():
    return {"message": "Logged out successfully"}


@router.get("/me", response_model=UserResponse)
def get_me(current_user: User = Depends(get_current_user)):
    return current_user
